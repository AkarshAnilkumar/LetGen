function submitText() {
    const textAreaValue = document.getElementById('text-area').value;
    // Do something with the submitted text, e.g., save to a database or perform some action.
    alert(`You entered:\n${textAreaValue}`);
  }
  